<!--
SPDX-FileCopyrightText: 2026 contributors
SPDX-License-Identifier: Apache-2.0
-->

# L02f2b: harness fix H1 and Q15 requalification

## Terms

- **Candidate** — `e1000_l02`, the driver written from the spec alone, as repaired in
  L02f2's round 1. **Reference** — Linux v6.12's `e1000`.
- **Receive hold** — QEMU's e1000 model delivers no received frame for one second after any
  write to RCTL (the receive control register); frames that arrive meanwhile are released
  in one burst when it ends. The manual describes no such behavior (L02f2, V6).
- **Leftover frames** — frames the peer's flood sent after the DUT's interface went down,
  delivered later in that burst.
- **Settling interval** — a documented, bounded wait after carrier returns and before the
  recovery pings.
- **H1** — L02f2's harness finding: `down-during-traffic` pinged as soon as carrier read 1,
  which could fall inside the receive hold, so a driver that reports carrier promptly failed
  at random.
- **Isolated run**, **claim**, **qualified**, **precondition** — as in the
  [L02d3 evidence](L02d3.md#terms); **d15** is L02d3's planted defect that keeps the receive
  ring indices across close and open.

See the [glossary](../GLOSSARY.md), the [plan](../IMPLEMENTATION-PLAN.md) (L02f2b), the
[L02f2 evidence](L02f2.md) (V6, H1) and the [notebook chapter](../notebook/L02f2b.md).

## Status

**Complete, 2026-09-25.** H1 is fixed and Q15 is requalified. On the fixed harness, in
isolated runs eight at a time, the reference passed `down-during-traffic` 10 of 10 and the
candidate 10 of 10, and d15 failed it 4 of 4 on the recovery pings, for the same reason as in
L02d3. The same load with the old harness reproduced H1 (candidate 4 of 8 failed). Q15 keeps
the scope "qualified, not traffic-specific", and the candidate's result for it is now PASS.

## The fix

Harness commit [`1f9f615`](https://github.com/curtisgalloway/driver-lab/commit/1f9f615)
(`l02harness.py` SHA-256 `57ef191b…`; `guest-init.sh` unchanged, `eccecebe…`):

- **Traffic through shutdown** stays as it was: both floods run until `ip link set eth0
  down` returns and are stopped only then, so the peer keeps sending into the outage.
- **Settling interval:** after carrier reads 1, the harness waits a fixed 3 s
  (`RECOVERY_SETTLE`, recorded as a host action), then pings once each way. No retry; the
  check names are unchanged.
- **New precondition,** checked from each run's register trace: "the pings afterwards
  began after the receive hold and the leftover burst" passes when the last RCTL write
  before the DUT's pings came at least 1 s earlier. Its detail records the time, the frames
  the model accepted between that write and the pings, and those accepted during the pings.
- Tests: the check (inside and outside the hold, reads and later writes ignored, no write)
  and the scenario's order with stand-in guests (floods stopped after the interface goes
  down, carrier, the 3 s wait, one ping each way).

**Why 3 s.** Both drivers rewrite RCTL at carrier-up, which starts a hold. In the 18 L02f2
traces (12 candidate, 6 reference) that write came 0.50 s (candidate) and 0.95 to 1.00 s
(reference) before the harness read carrier 1, so the hold ended at most 0.05 s after that
read. 3 s is the 1 s hold plus about 2 s of margin: it would still cover a carrier-up RCTL
write up to about 1.9 s after the read. This unit's traces confirm it (next section).

## Requalification

Private run `e1000-l02f2b-20260925-01`. The repetition counts and pass criteria were written
to the run's ledger before any run. All runs: `down-during-traffic` alone on freshly booted
guests, eight jobs in parallel (L02f2's failures came from runs eight or more at a time),
KVM; every run's `identities.json` records the kernel `0d96151b…`, QEMU `0cd4112a…`, busybox
`df12634c…`, and the declared harness and module. No run was repeated or discarded.

| Round | Harness | Driver (module) | Declared | Result |
| --- | --- | --- | --- | --- |
| `h0` load control | old (`3cc6765d…`, as L02d3 and L02f2) | candidate (`366782e4…`) | ×8, no criterion | 4 PASS, 4 FAIL, each on "DUT pings peer afterwards" only (seq 0 lost): H1 reproduces at this load |
| `h1` | fixed (`57ef191b…`) | reference (`43242751…`) | ×10, all PASS | **10 PASS** |
| `h1` | fixed | candidate (`366782e4…`) | ×10, all PASS | **10 PASS** |
| `h1` | fixed | d15 (`38a35afe…`) | ×4, all FAIL on the recovery pings for the intended reason | **4 FAIL**, each on exactly "DUT pings peer afterwards" and "peer pings DUT afterwards" (100 % loss); every precondition passed |

What the traces and captures show, seconds after `ip link set eth0 up`:

| | Candidate (`h1`) | Reference (`h1`) | Candidate, old harness (`h0`) |
| --- | --- | --- | --- |
| Last RCTL write before the pings | 0.52 | 2.01 to 2.07 | 0.52 |
| Carrier read 1 | 1.03 | 2.02 or 3.02 | 1.03 |
| Recovery pings start | 4.03 | 5.02, or 6.02 to 6.03 | 1.03 |
| Pings after that RCTL write (hold 1 s) | 3.505 to 3.510 s | 3.005 to 4.002 s | 0.508 to 0.510 s |
| Leftover frames accepted before the pings | 33 to 35, all at 1.52 to 1.53 | 47 to 57, the burst at 1.00 to 1.01 (end of the hold from open) | none: the burst comes after the pings start |
| DUT capture: first recovery request after the last flood-sized frame | 2.507 to 2.511 s | 1.99 to 5.02 s | 0.509 to 0.511 s |

- **Stimulus:** the existing precondition "traffic flowed both ways when the interface went
  down" (both tails written at least ten times in the second before) passed in all 24 `h1`
  runs.
- **Floods stopped, pings after the hold and the burst:** the new precondition passed in
  all 24 runs; the table's rows show the margin. While the DUT's pings ran, the model
  accepted 3 or 4 unicast frames (the replies, and in two reference runs one more), so no
  flood traffic overlapped them.
- **d15, the intended reason:** after the interface comes back up, d15 writes RDT = 0, equal
  to RDH, and no further RDT write until close: the receive ring has no buffers. The model
  reports two receiver overruns and accepts nothing for the pings. That matches L02d3's
  `q2-d15-down-during-traffic` exactly (four RDT writes, all 0; two overruns), so the check
  still fails a broken restart, and fails it on the recovery pings, not on a precondition.

| Claim | Result | Evidence |
| --- | --- | --- |
| Q15 The interface works after going down under traffic | **qualified, not traffic-specific** (unchanged scope) | reference 10/10 PASS (control); d15 4/4 FAIL on the pings afterwards, reception stalled after reopen. As in L02d3, d15 also fails a quiet down/up (Q14), so this shows the scenario catches a broken restart, not a fault only traffic triggers |

**Candidate result for Q15: PASS, 10 of 10 isolated runs** on the fixed harness. V6 stays
`benign` (model limitation and harness weakness); its harness part, H1, is resolved.

## What this means for L02f's two decisions

Q15 was the blocker both decisions named (plan, "Active blockers"). It now has a usable,
qualified result. Neither decision is met yet; both are reported in L02f3's acceptance
stage. *Evaluation complete* still needs L02f3's spec feedback (the E1 gap, the FWE
observation, flow control, L02s's carried notes). *Candidate qualified for the declared
scope* still has Q18 and Q24–Q26 unqualified, which the plan says to close as explicit
shortfalls. No blocking candidate finding is open.

## Review

One independent fresh-context reviewer read the diff and the run artifacts (report in the
run store under `review/`).

<!-- REVIEW-TABLE -->

## Limitations

- The settling interval is fixed at 3 s from 18 L02f2 traces and 24 of this unit's: one
  QEMU version, one host, KVM. A slower host or TCG may need more; the precondition fails
  loudly rather than letting a ping fall inside a hold unnoticed.
- The precondition sees only RCTL writes and the model's unicast-match events; it does not
  prove that no other model delay applies.
- Its detail counts leftover frames from the last RCTL write onward. For the reference the
  burst comes earlier, at the end of the hold from open, so its detail shows 1 frame; the
  counts above are from the whole trace after interface-up.
- Qualification is defect-specific, as in L02d3: d15 shows the check catches a restart that
  leaves the receive ring empty.
- The other nine scenarios were not rerun here. The change touches only
  `down-during-traffic`, but the harness hash changed: L02f3's acceptance stage runs every
  scenario on this harness.
- Host load was matched by concurrency (eight at a time, as before), not by measuring it.
