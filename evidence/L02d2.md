<!--
SPDX-FileCopyrightText: 2026 contributors
SPDX-License-Identifier: Apache-2.0
-->

# L02d2: QEMU harness, scenarios and planted defects

## Terms

- **Harness** — [`evals/e1000/harness/`](../evals/e1000/harness/): a host-side Python driver
  and a guest init script that boot two QEMU guests and run named scenarios against them.
- **DUT** (device under test) — the guest with QEMU's emulated Intel 82540EM, whose driver
  module a scenario loads. **Peer** — the second guest, with a virtio-net card.
- **Reference driver** — Linux v6.12's in-tree `e1000`. The **candidate** (`e1000_l02`, from
  L02e) is not run in this unit; L02f runs it.
- **Register trace** — QEMU's log of every guest read and write to the e1000's registers.
  **Phase** — the guest command or host action a register access happened during.
- **Deferred check** — a check a scenario registers and the harness evaluates after QEMU
  exits, from the trace or the packet captures.
- **Planted defect** (mutation) — a deliberate bug in a disposable copy of the reference
  driver. An **equivalent mutation** changes the code without breaking any rule the manual
  states, so no manual-based test can or should fail on it.
- **Review swarm** — the `review-swarm` skill: seven reviewer agents with narrow mandates, a
  mechanical check that drops any finding it cannot quote from the code, and a referee.
- **Operator** — the coordinating agent session that wrote the harness and this record.
- **SDM** — the Intel 8254x Software Developer's Manual pinned in L02a.

See the [glossary](../GLOSSARY.md), the [design](../QEMU-DIFFERENTIAL.md) (§5–§7; O3,
O5, and the harness halves of A3, A4, A6), the [plan](../IMPLEMENTATION-PLAN.md) (L02d,
L02d2), the [L02d1 evidence](L02d1.md), and the [lab notebook chapter](../notebook/L02d2.md).

## Status

**In progress — stopped early, 2026-09-24.** Built, run and reviewed; the review's chosen
fixes are applied and unit-tested, but **the fixed harness has not run on the test host**.
SSH to the host failed before the final runs, and the user then asked to move the work to
its own repository. The Acceptance section below reports runs r003 to r005 and round 2 of the
planted defects, all on the code *before* the review fixes.

Remaining to complete the unit:

1. Sync the harness to the test host; run the reference suite twice (r006, r007), and all
   five planted defects again (round 3).
2. Confirm the new checks behave: no leftover frames in the reference; m5's link-flap and
   reload failures now name the leftover-frame cause; link-loss-tx's carrier check passes;
   ring-wrap still counts at least 3 real wraps.
3. A fresh review of the fixes (the diff since the review, against the findings table below).
4. Update this file's Acceptance and Measures, the plan, and the notebook; checkpoint.

## Starting state

- Branch `driver-porting/l02d2` in its own worktree, from `origin/main` at [`d63e3f1`](https://github.com/curtisgalloway/public-skills/commit/d63e3f1) (the
  merge of PR #90, L02d1). No local changes.
- Reused on the test host without rebuilding: the L02e kernel build (v6.12,
  `x86_64_defconfig`, `CONFIG_E1000=m`, gcc-14) and the L02d1 identities below.

## Artifact identities

| Artifact | SHA-256 |
| --- | --- |
| Guest kernel `bzImage` (v6.12) | `0d96151ba731e125914465e1debdc05f1d9f30a6a775fdffa32f277a412ead88` |
| Reference module `e1000.ko` | `43242751f287e6aa6a19eac720c907bb995fdf5c27bf8ca75fa2ad7f4bd049b0` |
| `qemu-system-x86_64` 10.2.1 (`1:10.2.1+ds-1ubuntu3.2`) | `0cd4112a8f0cb891eb7c10e8df38c9dfeec8c7389bb22db6aa425f0d6fe733dc` |
| `busybox-static` 1.37.0 | `df12634c17fcdca839ae5dc47d7627b7558511f7645de7c99ccf097a0f28ed5b` |
| m1 receive tail one early | `0fa1606a17af01c837c31df4c7db8dfa6205a30bc1f8a879779523ef7292b2e3` |
| m2 transmit descriptors without RS | `6ef687abf3f9338baa243dc1cc78a45ed36e2be4e730a4f50f64ed303395718c` |
| m3 interrupt mask not restored after polling | `54e0953720838ee56ab97f433afac1eb84aa5fded3470e4e3b354e422d7166ed` |
| m4 receive ring length one descriptor short | `b9d358f0156d35317c5c19ff1082d745289bcc8681b3767b1c178cc4c75010d5` |
| m5 receive tail written only on the first fill | `b951f122055835c6fa468237517398442f30b3f911b3f1dd1655e6db4cdead47` |

Each run's `identities.json` also hashes its initramfs and the harness files. The planted
defects are one exact-string edit each to a copy of the reference source, built out of tree
against the same kernel build with no warnings; rebuilding gave identical modules. The edit
script and diffs quote GPL source, so they stay in the private run store; the defects are
described here in words.

## What was built

- **Nine scenarios** beyond `smoke`, covering the design's §5 list: `frame-sizes`,
  `ring-wrap`, `rx-overrun`, `link-flap`, `link-loss-tx`, `stop-start`,
  `down-during-traffic`, `reload`, `itr`. `--scenario all` runs the ten in order; a suite run
  takes about 133 s with KVM. Each is described in the [harness README](../evals/e1000/harness/README.md#scenarios).
- **A kernel-log check after every scenario**, taken after the driver is unloaded: warnings,
  call traces, oopses, transmit hangs, netdev watchdog timeouts, interrupt storms, DMA-API and
  allocation failures fail the scenario. This closes the L02d1 limitation.
- **Trace decoding and per-phase grouping for L02f:** every register access decoded to the
  manual's register name (I/O-window accesses resolved to the register `IOADDR` selected) and
  labeled with its scenario and phase, in `dut-regtrace.jsonl`; per-phase access counts in
  `trace-phases.json`.
- **Trace rules from the manual**, checked over the whole trace: at least 1 µs between a
  global reset and the next register access (SDM 13.4.1); ring lengths nonzero multiples of
  128 (13.4.27, 13.4.38); ring bases 16-byte aligned; tail writes inside the ring (3.2.6,
  3.4); head writes only while that direction is disabled (13.4.28, 13.4.39); ITR's reserved
  bits clear (13.4.18). A reset returns lengths and enables to power-on values (14.7).
- **Deferred checks** that prove each scenario created its condition: tails wrapping, the
  DUT's frame lengths in the peer's capture, traffic in the trace in the second before a link
  or interface drop, ITR read-back against the last write.

## Decisions

- **ITR read-back (L02b finding C-2).** Read through the memory BAR with busybox `devmem`, and
  compared by a deferred check with the last ITR write in the trace, or with 0 after a global
  reset. It tests the device model and the driver's write together without assuming a value;
  spec revision 4 allows ITR = 0.
- **Receive overrun in emulation.** QEMU's e1000 does not drop and count frames when the
  receive ring is full; it stops accepting them, and the backlog pushes back to the peer. There
  is no missed-packet count or overrun cause to observe. The scenario instead masks the DUT's
  interrupts (the harness writes IMC, then restores IMS), floods it, and requires the receive
  head to reach the tail (SDM 3.2.6) before reception resumes. A model limitation for the
  proposed `[emulated]` evidence class.
- **m1 is an equivalent mutation.** The design's first example, "receive tail written one
  descriptor early", hands the device one fewer buffer. The manual lets software hand over
  any number of descriptors (SDM 3.2.6), and the reference already holds one back. It is kept
  in the record, undetected as predicted, and m5 plants the defect the example was reaching
  for: the tail written only on the first fill after load.
- **Floods are `ping -i 0.001`.** This busybox's `nc` has no UDP mode, and `ping -i 0` hangs
  the guest. Each flood is checked to be running, and the trace must show traffic in the
  second before the link or interface drops.
- **Neighbor entries are flushed after a link outage**, after being recorded: pings sent while
  the link was down leave the ARP entry unresolved, and the next ping can be lost waiting for
  a fresh resolution. That is the stack's behavior, caused by the scenario.
- **DUT memory 3 GiB**, so that memory above 4 GiB exists for L02e's DMA-mask question (M2).

## Acceptance

| Criterion (plan, L02d) | Result |
| --- | --- |
| One command runs the whole suite unattended | `l02harness.py run ... --scenario all`; r003, r004, r005 ran start to finish without intervention (133 s). |
| The reference passes, or each failure is explained by the manual | r003, r004 (under a 12-VM load) and r005: all ten scenarios, the trace rules and the capture checks PASS. r001 and r002's failures were the harness's and were fixed (see below). |
| Every planted defect is detected | m2, m3, m4 and m5 FAIL (below). m1 passes and is an equivalent mutation (Decisions). |
| Outputs are stored per run with identities | Every run directory holds `verdicts.json`, `identities.json`, both traces, the labeled trace, both captures, consoles, command logs and per-scenario kernel logs. |

### Planted defects (round 2, all run in parallel with r004)

| Defect | Scenarios that FAIL | What caught it |
| --- | --- | --- |
| m1 receive tail one early | none | equivalent mutation |
| m2 transmit descriptors without RS | all ten | pings lost; "Detected Tx Unit Hang" in the kernel log |
| m3 interrupt mask not restored after polling | all ten | pings lost; netdev watchdog timeouts in the kernel log |
| m4 receive ring length one descriptor short | ring-wrap, rx-overrun, link-loss-tx, down-during-traffic; trace rules | RDLEN 0xff0 and RDT 0xff in a 255-descriptor ring; lost traffic once the ring wraps. `smoke` passes |
| m5 receive tail written only on the first fill | ring-wrap, rx-overrun, link-flap, link-loss-tx, stop-start, down-during-traffic, reload | reception stops once the first fill is used, and after any reopen. `smoke` passes |

m4 and m5 are the cases the design's scenario list exists for: both pass the smoke test.

### Observations for L02f (reference driver, r005)

- **Reset path (L02e item M3):** all 57 global resets went through the I/O window, none
  through the memory BAR; the gap to the next register access was about 6 ms each time.
- **DMA above 4 GiB (M2):** no ring base above 4 GiB in any run, although the DUT has memory
  there.
- **ITR:** reads back 195 (about 20,000 interrupts/s); the reference writes 195, 205, 279,
  434 and 976 over a run.
- **Link loss (L4):** TDH equalled TDT after 8 s without link: no transmit left pending.
- **Link change:** four ICR reads with the link-change cause set in `link-flap`.

Unit tests (after the review fixes): 35 of 35 pass. At r005: 26 of 26 pass (`python3 -m unittest discover -s evals/e1000/harness/tests`), 13
of them new: the decoder on every access kind, phase labeling and its priority order, each
trace rule against its violation and the reset semantics, tail-wrap counting, ITR read-back,
the traffic-before check, the frame-size capture check, the kernel-log filter, and the suite
list. They run in `.github/workflows/checks.yml`.

## Failed and superseded attempts

| Runs | What happened |
| --- | --- |
| r001 | 9 of 10 passed, but the floods were `nc -u`, which this busybox does not have: rx-overrun printed a usage error and two scenarios passed with no traffic. |
| r002 | Ping floods. rx-overrun failed its check for a QEMU overrun event (the model pushes back instead); link-loss-tx lost the first ping after link-up to an unresolved ARP entry. |
| mut-*-r1 | m1 to m4. The first failing `ping -A` ran into the command timeout (without replies it sends one request a second), the harness declared the DUT dead, and every later scenario failed on that. Now bounded with `-w 60`. |

All runs are kept in the run store.

## Review

Two independent reviews ran on the code as it stood after r005; reports are in the run
store's `review/` directory, with reviewer transcript paths in its ledger.

**1. Review swarm** (base [`d63e3f1`](https://github.com/curtisgalloway/public-skills/commit/d63e3f1) against the working tree; the three harness files).
All seven arms (security, correctness, compat, docs, history, conventions, perf) delivered.
The mechanical check verified all 14 findings; the referee dropped 1 (the new 3 GiB DUT
memory default, which is intended) and merged 1, leaving 1 medium and 11 low. The user chose
which to fix.

| ID | Sev | Finding | Resolution |
| --- | --- | --- | --- |
| F12 | medium | A monitor (QMP) call on a dead QEMU became a harness ERROR, repeating L02d1's N1 | `set_link` checks whether QEMU exited and raises a guest failure; otherwise a harness error. Two tests |
| F13 | low | New terms not in the repository glossary | Planted defect, Phase and Deferred check added |
| F11 | low | README said every scenario loads the driver; the self-tests do not | "Every suite scenario" |
| F8 | low | README and a comment claimed §5's order | Now: §5 plus `link-loss-tx`, not in §5's order |
| F14 | low | The labeled trace was 66 MB per run uncompressed | Written as `dut-regtrace.jsonl.gz` |
| F4 | low | A tail reset to 0 counted as a ring wrap | Wraps counted within one life of a ring; writes of 0 skipped. Test |
| F3 | low | Float subtraction made an exact 1 µs gap read 0.95 µs | Gaps rounded to whole microseconds (the trace's resolution). Test |
| F6 | low | `dut-dmesg-NN-smoke.txt` kept its name with new content | Not changed (user): recorded here; no tool reads old runs |
| F2 | low | An IMS read of 0 counted as a failed read | `ims is not None` |
| F5 | low | A crashing deferred check overwrote the scenario's error | The original error is kept in front |
| F9 | low | `--dut-mem` help said DMA memory is put above 4 GiB | Says it is made available |
| F10 | low | The `all` test never ran the expansion | Test runs `main()` with `run` patched |

**2. A fresh scenario-coverage reviewer**, reading r005 and the round-2 defect runs (not the
GPL diffs): 8 scenarios did what they claim; 6 medium and 5 low findings. Chosen by the user:

| ID | Sev | Finding | Resolution |
| --- | --- | --- | --- |
| C1 | medium | Frames left over from an earlier scenario's stalled flood failed m5's link-flap and first reload | Every bring-up now checks that at most 4 unicast frames arrived during it, naming the cause. Test |
| C2 | medium | Wrap counter scored load and unload as wraps | Same fix as F4 |
| C3 | medium | link-loss-tx never held transmits through the outage, and never checked carrier fell | Carrier check added; L4 recorded as not reproducible in emulation (the model drains TX with the link down) |
| C4 | medium | The ITR check compares the model with itself; the notebook claimed it tests the driver | Relabeled a model and decoder check; the driver's ITR writes after its last reset are recorded. Notebook corrected |
| C5 | medium | The DMA-mask probe (M2) did not happen: no ring above 4 GiB | Recorded: not probed for the reference; L02f observes the candidate |
| C9 | medium | Most checks have never been shown able to fail (design §7) | **Not fixed** (not chosen): open limitation below |
| C6 | low | Link-change observation counted bring-up | Counts only after the link drops. Test |
| C7 | low | The 1 µs rule cannot fail for I/O-window resets | Check detail names the windows; open limitation |
| C8 | low | stop-start never stops a live link | Open limitation (down-during-traffic covers it once) |
| C10 | low | smoke cannot show the MAC came from the EEPROM | Open limitation (already in the README) |
| C11 | low | The dry-ring read had no trace backing | Deferred check: frames accepted and no RDT write while masked. Test |

Both reviewers were fresh Claude Opus 5.5 subagents, the same model family as the operator.
**The fixes themselves have not been reviewed.**

## Measures

- Operator time for the unit: about 17:26 to 20:40, 2026-09-24 (Pacific), one session,
  stopped early. Model cost was not measured.
- Host time: a suite run takes about 133 s with KVM; five defect runs and a reference run in
  parallel took about 4 minutes.
- Human review effort: two decisions (which findings to fix; wrap up).

## Open limitations

- The kernel-log patterns are a fixed list; a fault that logs something else passes.
- The 1 µs reset rule measures QEMU's processing times, not bus timing; under KVM each
  register access costs microseconds, so a driver with no wait could still pass it.
- Receive overrun counters (MPC, RNBC) and the overrun cause cannot be observed in QEMU.
- `--accel tcg` still untested.
- Packet checks match frames by content only: `filter-dump` timestamps are not on the host
  clock (28,796 s apart in the L02d1 capture checked).
- The peer uses the same kernel as the DUT.
- **Most checks have never been shown able to fail** (C9): frame-sizes' capture checks,
  itr, link-flap's carrier check, stop-start, reload, and the gap, base, head and ITR trace
  rules failed only as a side effect of total transmit loss or not at all. Two §7 examples
  are half covered (RS only; receive ring only). Proposed defects: pad bit cleared with no
  software padding, TDLEN one short, LSC left out of IMS, ITR never written, no post-reset
  delay. FCS insertion is invisible in these captures.
- The 1 µs reset rule cannot fail for resets through the I/O window, which is how the
  reference resets (C7). stop-start never stops a live link (C8).
- L02e item L4 cannot be reproduced in QEMU; M2 was not probed with the reference, which
  uses a 32-bit DMA mask here.
- **The review fixes are untested on the host and unreviewed** (Status).
